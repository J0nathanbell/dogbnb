<div class="justify-center items-center">
  <h1 class="font-bold text-black">User Profile Page</h1>
  <p class="text-gray-900 leading-none"><%=@user.firstname.capitalize()%> <%= @user.lastname.capitalize()%></p>
  <p class="text-gray-900 leading-none"><%=@user.email%></p>
  <p class="text-gray-600">Account created at: <%= @user.created_at %></p>
  <button class="bg-white hover:bg-gray-100 text-gray-800 font-semibold py-2 px-4 border border-gray-400 rounded shadow"><%= link_to 'Home', root_path%></button>
  <button class="bg-white hover:bg-gray-100 text-gray-800 font-semibold py-2 px-4 border border-gray-400 rounded shadow"><%= link_to "Edit", edit_user_path(@user)%></button>
  <%= button_to "Log out", logout_path, method: :delete, class: "bg-white hover:bg-gray-100 text-gray-800 font-semibold py-2 px-4 border border-gray-400 rounded shadow" %>
  <%= button_to "Delete User", destroy_path(@user), method: :delete, data: { confirm: "Are you sure you want to delete this user?" }, class: "bg-white hover:bg-gray-100 text-gray-800 font-semibold py-2 px-4 border border-gray-400 rounded shadow" %>
</div>
